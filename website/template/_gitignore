# Build output: dist/ is what ships to the edge, build/ holds the Worker bundle.
dist/
build/
static/site.css
src/generated/
node_modules/
.site-capture/

# Never commit credentials. Task & Tool delivers env to the machine; on your
# own computer use .env (kept out of git here).
.env
.env.*
!.env.example
.wrangler/
.DS_Store
# files sent in chat and screenshots the AI shows you (attach_files); never part of the site
uploads/
